import type { Config } from "prettier";

const config: Config = {
  printWidth: 80,
  useTabs: true,
  semi: true,
  singleQuote: false,
  quoteProps: "as-needed",
  jsxSingleQuote: false,
  trailingComma: "none",
  bracketSpacing: true,
  arrowParens: "avoid",
  endOfLine: "lf",
};

export default config;
